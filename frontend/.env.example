# Optional: override API base URL used by axios in the SPA
# Useful for mobile testing in development (phone cannot reach your computer's localhost)
# Example for LAN dev:
# VITE_API_BASE_URL=http://192.168.1.50:8000

# In production you typically leave this unset so the SPA calls same-origin APIs
# VITE_API_BASE_URL=
